{
  "package": "zustand",
  "repo": "https://github.com/pmndrs/zustand",
  "sourceStrategy": {
    "type": "git-tag",
    "tagPattern": "v{version}"
  },
  "entryPoints": ["src/vanilla.ts"],
  "buildCommand": "printf 'interface ImportMeta { env?: { MODE?: string } }' > env.d.ts && npx tsc src/vanilla.ts env.d.ts --outDir build --module esnext --target es2020 --moduleResolution node",
  "versionPairs": [
    {
      "v1": "4.4.0",
      "v2": "4.5.0",
      "description": "Minor: added getInitialState, formatting changes",
      "expectMatchDespiteModification": [
        {
          "function": "createStore",
          "reason": "TypeScript source has trailing comma differences that change body hash, but compiled JS function body is identical"
        }
      ]
    },
    {
      "v1": "4.5.0",
      "v2": "5.0.0",
      "description": "Major: removed destroy/deprecated default export, setState change",
      "expectMatchDespiteModification": [
        {
          "function": "createStore",
          "reason": "TypeScript source has trailing comma and type annotation differences that change body hash, but compiled JS function body is identical"
        }
      ]
    }
  ]
}
