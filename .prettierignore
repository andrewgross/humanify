# Byte-exact fixtures: the WP4.1 replay gate compares these cache entries
# to the bytes the TS JSON.stringify wrote — a reformat breaks the gate.
test/parity/wp41-replay/

# WP4.2: the probe's output is regenerated byte-for-byte by
# test/parity/wp42-probe.ts; the gate fixture is cut from real dumps.
test/parity/wp42-vectors.json
test/parity/wp42-gate-fixture/
