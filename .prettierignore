# Byte-exact fixtures: the WP4.1 replay gate compares these cache entries
# to the bytes the TS JSON.stringify wrote — a reformat breaks the gate.
test/parity/wp41-replay/

# WP4.2: the probe's output is regenerated byte-for-byte by
# test/parity/wp42-probe.ts; the gate fixture is cut from real dumps.
test/parity/wp42-vectors.json
test/parity/wp42-gate-fixture/

# WPB.4: probe outputs regenerated byte-for-byte by test/parity/wpb4-*.ts;
# the stats fixtures are real --stats-json files the gate round-trips.
test/parity/wpb4-cli-surface.json
test/parity/wpb4-vectors.json
test/parity/wpb4-stats-schema.json
test/parity/wpb4-scenarios.json
test/parity/wpb4-stats-*.json
# The env-reads shape corpus: its layout (line numbers, an 80-unit snippet)
# IS the test input.
test/parity/wpb4-env-reads-cases.js

# WP4.4/4.5: the passes fixture corpus, regenerated by
# test/parity/wp445-harvest-collect.py from the instrumented TS unit tests.
test/parity/wp445-fixtures.json

# Library-freeze carry (#32): the probe output is regenerated byte-for-byte
# by test/parity/library-carry-probe.ts; the inputs are regime texts whose
# banner offsets ARE the test input.
test/parity/library-carry.json
test/parity/library-carry-inputs.json
