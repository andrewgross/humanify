# 16 — Findings queue: TS bugs and quirks the port surfaced

**The one list.** Every TS bug or quirk a parity gate or a port surfaced
lands here, with its status. Policy (Andrew, 2026-09-20): fix real bugs
rather than copy them, TS side first so the eval can measure the fix; a
quirk that changes no output can wait for the cutover. A fix that changes
decisions forces an oracle re-cut — batch them.

| #   | found             | what                                                                                                                                                   | effect on output                                                       | status                                                                     |
| --- | ----------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------- | -------------------------------------------------------------------------- |
| 1   | WP1.4, 2026-09-19 | Babel never visits `OptionalCallExpression` with a `CallExpression` visitor: the graph drops every `x?.()` callee edge                                 | sized ZERO on the four pairs (`--visit-optional` sizing flag, 0ce7e16) | parked; the Rust reproduces it                                             |
| 2   | WP1.5, 2026-09-19 | graph-time Bun factory classification never fires on real bundles (the beautifier splits the `{exports:{}}` marker)                                    | ZERO: vendored bodies are never renamed                                | closed, no fix needed                                                      |
| 3   | WP2.3, 2026-09-20 | twin-gates dump's `cascadeConflicts` flushed stale                                                                                                     | dump only                                                              | FIXED both sides (b8b27e2)                                                 |
| 4   | WPB.2, 2026-09-20 | `writePartitions` span-converted the structuralSignature family's path keys into one degenerate key                                                    | dump only                                                              | FIXED in TS (c64632d)                                                      |
| 5   | 2026-09-20        | `hash::serialize`'s per-class private-name slot map is dead code (privates serialize verbatim in both implementations)                                 | none today; fixing it shifts hashes → WP3.2 private-name transfer      | open, decide at WP3.2                                                      |
| 6   | WPB.2, 2026-09-24 | the vendor LLM fallback step was never compared on real data (the TS applied 17 names on 2.1.215-216)                                                  | unknown                                                                | open: re-gate recipe in `/work/rust-port/gates/vendor-naming/README.md`    |
| 7   | WP4.1, 2026-09-24 | the dump's `cache-keys.jsonl` drops callee `snippet`, which IS part of the cache key: 30–50% of rows per pair can't be re-derived from the dump        | dump only                                                              | open: fix `cacheKeyMaterialRow` (src/dump/artifacts.ts) at the next re-cut |
| 8   | WP4.1, 2026-09-24 | the LLM cache key includes callee `snippet` although no prompt shows it: a callee body change busts the cache even when the prompt bytes are identical | cache efficiency only (more live asks, more LLM re-rolls on warm runs) | open: candidate lever — key on the prompt bytes; measure with the eval     |
| 9   | WP4.1, 2026-09-24 | the rate limiter's per-minute check is not atomic with its timestamp record (a concurrent burst all passes)                                            | none: production never sets requests-per-minute                        | reproduced; fix at cutover                                                 |
| 10  | WP4.1, 2026-09-24 | the rate limiter's retry check never matches the SDK's final "Connection error." / "Request timed out." strings; only the SDK's own two retries apply  | resilience only                                                        | reproduced; fix at cutover                                                 |
